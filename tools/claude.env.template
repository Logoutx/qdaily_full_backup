# Copy to ~/Library/Application Support/qdaily/claude.env and fill in.
# Generate the token with:  claude setup-token
# (long-lived, subscription-backed — unlike the keychain OAuth access token,
#  which expires and is NOT auto-refreshed for headless `claude -p` runs.)
CLAUDE_CODE_OAUTH_TOKEN=
